{
  "name": "sherpa-demo-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"agent tests pending\"",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@sherpa/guardrails": "workspace:*",
    "@sherpa/intent": "workspace:*",
    "@sherpa/policy": "workspace:*",
    "viem": "^2.51.0",
    "tsx": "^4.22.3",
    "typescript": "^5.9.3"
  }
}
